export type Dimensions = { width: number; height: number };
export type Question = { id: number; question: string; answer: string | number };

export type IQuestion = { id: number; value: string; answer: string | number };
